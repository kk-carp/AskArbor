---
description: MVP 自动化测试范围与隔离验收规范
globs: tests/**/*.py
alwaysApply: false
---

# 测试规范

- 使用 pytest；测试名采用 `test_<行为>_<结果>`，一个测试验证一个主要行为。
- 单元测试不得访问真实 DeepSeek、下载真实 BGE-M3 或依赖公网；使用 mock 或小型替身。
- 集成测试使用独立测试数据库，不复用开发数据库；每个测试清理自己创建的数据。
- 必测角色映射：`student`、`employee`、`teaching` 和非法角色。
- 必测检索隔离：`student` 永不返回 `company` 的正文、标题、ID 或来源。
- 必测未命中：`hit=false`、`sources=[]`，并断言 DeepSeek 客户端未被调用。
- 必测命中：来源来自数据库召回结果，不能采信模型输出的虚假文档名。
- 必测文档失败：空文件、扫描 PDF、解析失败不会留下可检索切片。
- 对阈值边界分别测试低于、等于和高于最低分。
- 测试应验证外部行为，避免依赖函数内部实现细节。
- 修复缺陷时先添加能复现问题的回归测试。
