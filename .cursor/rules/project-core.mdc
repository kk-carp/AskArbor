---
description: FDE 统一知识助手的全局范围与架构约束
alwaysApply: true
---

# 项目核心规范

- 当前目标是 FDE 课程 MVP；以 `docs/需求分析.md`、`docs/技术选型.md`、`docs/系统架构设计.md` 为需求与架构基准。
- MVP 只实现文档入库、BGE-M3 dense 向量、按空间隔离检索、DeepSeek 作答、真实来源和未命中拒答。
- 未经明确要求，不实现登录、用户表、工单、负责人、Redis、Celery、对象存储、独立前端或微服务。
- 保持单体 FastAPI + PostgreSQL/pgvector；BGE-M3 在 API 进程启动时加载一次。
- `student` 只能检索 `student`；`employee` 只能检索 `company`；`teaching` 可检索两者。
- 允许空间必须由服务端根据角色计算，客户端不得直接决定 `space_ids`。
- 空间过滤必须进入向量检索 SQL 的 `WHERE`，禁止检索全量后在 Python 中过滤。
- 未命中或低于阈值时返回固定拒答，不调用 DeepSeek。
- 来源必须由数据库召回记录生成，不让模型生成或修改来源。
- 失败文档不得参与检索；系统故障和知识库未命中必须使用不同错误语义。
- 优先最小、直接、可测试的实现；不要为未来需求建立空模块或通用抽象。
- 新依赖必须有当前 MVP 的直接用途；能用现有标准库或已选依赖完成时不要新增。
